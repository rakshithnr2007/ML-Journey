# NumPy

My NumPy learning notes and practice notebooks, covering the essential concepts required for Data Science and Machine Learning.

## 📚 Topics Covered

### 1. NumPy Fundamentals

* Creating NumPy arrays
* 1D, 2D and 3D arrays
* `ndim`
* `shape`
* `size`
* `dtype`
* `itemsize`

### 2. Array Creation

* `np.array()`
* `np.zeros()`
* `np.ones()`
* `np.empty()`
* `np.arange()`
* `np.linspace()`
* Identity matrices
* Random arrays

### 3. Indexing & Slicing

* 1D indexing
* 2D indexing
* 3D indexing
* Array slicing
* Fancy indexing
* Boolean indexing

### 4. Array Manipulation

* `reshape()`
* `ravel()`
* Transpose
* `expand_dims()`
* `append()`
* `delete()`
* `put()`
* `flip()`
* `repeat()`
* `swapaxes()`

### 5. Broadcasting

* Broadcasting concept
* Broadcasting rules
* Operations between arrays of different shapes

### 6. Mathematical Operations

* Arithmetic operations
* `sqrt()`
* `sin()`
* `log()`
* `exp()`
* Rounding functions

### 7. Statistical Operations

* `mean()`
* `median()`
* `std()`
* `var()`
* `min()`
* `max()`
* `sum()`
* `prod()`
* Percentiles
* Histograms
* Correlation

### 8. Sorting & Searching

* `sort()`
* `where()`
* `argmax()`
* `argmin()`
* `isin()`

### 9. Stacking & Splitting

* `hstack()`
* `vstack()`
* `concatenate()`
* `hsplit()`
* `vsplit()`

### 10. Set Operations

* Union
* Intersection
* Difference
* Symmetric difference

### 11. Cumulative Operations

* `cumsum()`
* `cumprod()`

### 12. Missing Values

* `np.nan`
* `isnan()`

### 13. Matrix Operations

* Matrix transpose
* Dot product
* Matrix multiplication
* `@` operator
* Element-wise operations

### 14. Random Module

* Random number generation
* Random arrays
* Random sampling

### 15. NumPy Tricks & Useful Operations

* Array manipulation techniques
* Shape manipulation
* Advanced indexing techniques
* Efficient NumPy operations

### 16. ML-Related NumPy

* Sigmoid function
* Mean Squared Error (MSE)
* Basic mathematical operations used in ML

---

## 📂 Notebooks

| Notebook             | Description                                  |
| -------------------- | -------------------------------------------- |
| `numpy_Basics.ipynb` | Core NumPy concepts and operations           |
| `Numpy_Depth.ipynb`  | Deeper NumPy concepts and array operations   |
| `Numpy_tricks.ipynb` | Useful NumPy tricks and practical techniques |

---

## 🎯 Learning Goal

The goal was not to learn every NumPy function, but to build a strong practical foundation for:

```text
NumPy
  ↓
Pandas
  ↓
Data Analysis
  ↓
Machine Learning
```

## ✅ Status

**NumPy — Essential concepts completed**

I will revisit NumPy when a future Data Science or Machine Learning project requires additional concepts.

